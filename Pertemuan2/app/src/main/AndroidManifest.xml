<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="xyz.deonico.pertemuan2">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tugas4.myCV"></activity>
        <activity android:name=".tugas3.lifecycleCallbackTwoActivitiesSecond" />
        <activity android:name=".tugas3.lifecycleCallbackTwoActivities" />
        <activity android:name=".tugas2.implicitIntents" />
        <activity android:name=".tugas1.tugas1Second" />
        <activity android:name=".tugas1.tugas1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>